// https://astro.build/config
import { defineConfig } from "astro/config";

export default defineConfig({
  // Static output — ideal for Cloudways public_html and other static hosts
  output: "static",
});
